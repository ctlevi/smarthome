{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "upload": "zip -r lambda.zip .; aws lambda update-function-code --function-name SmartHomeGraphQL --zip-file fileb://lambda.zip"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.4.6",
    "graphql": "^0.7.1",
    "lodash": "^4.16.4",
    "moment": "^2.15.1"
  }
}
